Spring事务管理分为两大类:
	编程式事务管理
		*手动编写代码进行事务管理(很少使用)
	声明式事务管理
		*基于TransactionProxyFactoryBean的方式(很少使用)
			*需要为每一个进行事务管理的类配置一个TransactionProxyFactoryBean进行增强
		*基于AspectJ的XML方式(经常使用)
			*配置好后，类上面不需要添加注解
		*基于注解方式(经常使用,很方便)
			*配置简单，在业务类中添加@Transaction注解